# 效用计算修改问题分析报告

**日期**: 2026-03-07
**问题描述**: 修改效用计算方式从 `U = Revenue - Cost` 改为 `U_m = log(1 + rho_m * (V_m * D_m - Cost))` 后，完成的联盟价值非常低

---

## 目录
1. [配套函数不一致问题](#1-配套函数不一致问题)
2. [理论问题深度分析](#2-理论问题深度分析)
3. [数值示例对比](#3-数值示例对比)
4. [建议方案](#4-建议方案)

---

## 1. 配套函数不一致问题

### 1.1 Huo2025算法使用旧版效用计算

**问题文件**: `comalg/Com_Huo2025/Value_utility.m`

**代码位置**: 第85行
```matlab
agentutility = revenue - cost;  % 直接线性计算
```

**问题描述**:
- Huo2025算法的 `Value_utility.m` 和 `Value_order.m` 仍然使用**旧版线性效用公式**
- 没有使用 `UtilityEvaluator.calc_agent_total_utility` 的新对数平滑公式
- 导致同一个联盟结构在不同算法中计算出的效用值**数量级不同**

**影响范围**:
- `Huo2025_main.m` (算法ID=2)
- `Value_order.m` 中的任务选择逻辑
- 所有依赖Huo2025效用计算的函数

---

### 1.2 全局评估函数使用线性计算

**问题文件**: `Main_fun/UtilityEvaluator.m`

**代码位置**: 第240-315行，`evaluate_coalition_metrics` 函数
```matlab
% 第311行：全局净效用 = 总完成价值 - 总成本
global_net_utility = total_completed_value - total_global_cost_sum;
```

**问题描述**:
- `evaluate_coalition_metrics` 用于全局性能评估，采用**线性公式** `Revenue - Cost`
- 而个体效用计算 `calc_agent_total_utility` 采用**对数平滑公式**
- 导致: **∑(个体效用) ≠ 全局净效用**

**影响范围**:
- `ResultProcessor.m` 的历史记录（`history_data.coalition_utility`）
- 所有调用 `evaluate_coalition_metrics` 的算法：
  - `SA_Value_TabuEnhanced_global_main.m`
  - `Qi2023_main.m`
  - `Huo2025_main.m`
  - `Fang2025_global_main.m`

**矛盾示例**:
```
假设3个智能体分别获得对数效用: [2.5, 3.0, 2.8]
个体效用之和 = 8.3

但全局评估计算:
总收益 = 1500, 总成本 = 600
全局净效用 = 900

数量级完全不匹配！
```

---

### 1.3 效用差值计算的不对称性

**问题文件**: `SA/Preference_gain.m`

**代码位置**: 第38-39行, 第125行
```matlab
% 计算两个状态的效用
u_n_Q = UtilityEvaluator.calc_agent_total_utility(SC_Q, ...);
u_n_P = UtilityEvaluator.calc_agent_total_utility(SC_P, ...);

% 第125行：计算效用差值
deltaU = delta_self + sum_gain_new - sum_loss_old + sum_diff_other;
```

**问题描述**:
对数函数**不满足可加性**，导致效用差值失去物理意义：

```
线性公式（旧版）:
  U_total = U_task1 + U_task2 + U_task3
  ΔU = U_Q - U_P 可以拆解为单个任务的变化之和

对数公式（新版）:
  log(1+x1) + log(1+x2) ≠ log(1 + x1 + x2)
  ΔU = log(1+xQ) - log(1+xP) ≠ 任务级变化之和
```

**影响**:
- BMBT偏好计算中的 `sum_gain_new - sum_loss_old` 项会产生**系统性偏差**
- Metropolis准则 `exp(ΔU/T)` 失去统计力学意义
- 禁忌搜索的邻域评估失准

---

## 2. 理论问题深度分析

### 2.1 对数平滑导致值域压缩

**旧公式**:
```matlab
U = V*D - Cost
```
- **值域**: `(-∞, +∞)`
- **线性度**: 完全线性
- **敏感度**: 对大小变化同等敏感

**新公式**:
```matlab
inner_val = rho_m * (V*D - Cost)
U_m = log(1 + max(inner_val, -1 + tol))
```
- **值域**: `[log(tol), +∞)` ≈ `[-20.7, +∞)`
- **非线性度**: 对数递减
- **敏感度**: 小值敏感，大值钝化

**数值对比**:

| 净收益 (V*D - Cost) | 旧公式效用 | 新公式效用 (rho=1) | 压缩比 |
|---------------------|-----------|-------------------|--------|
| 1000                | 1000      | log(1001) ≈ 6.91  | 144.7x |
| 100                 | 100       | log(101) ≈ 4.62   | 21.6x  |
| 10                  | 10        | log(11) ≈ 2.40    | 4.2x   |
| 1                   | 1         | log(2) ≈ 0.69     | 1.4x   |
| 0.1                 | 0.1       | log(1.1) ≈ 0.095  | 1.05x  |
| 0                   | 0         | log(1) = 0        | 1x     |
| -0.5                | -0.5      | log(0.5) ≈ -0.69  | 1.4x   |
| -0.9                | -0.9      | log(0.1) ≈ -2.30  | 2.6x   |
| -0.99               | -0.99     | log(0.01) ≈ -4.61 | 4.7x   |
| -1 (截断阈值)       | -1        | log(1e-9) ≈ -20.7 | 20.7x  |
| -10 (被截断)        | -10       | log(1e-9) ≈ -20.7 | 0.48x  |
| -100 (被截断)       | -100      | log(1e-9) ≈ -20.7 | 0.21x  |

**关键发现**:
1. **高收益任务效用被严重压缩**: 净收益1000的任务，效用从1000降到6.91，**压缩144倍**
2. **低收益任务效用接近原值**: 净收益0.1的任务，效用从0.1降到0.095，几乎不变
3. **亏损任务效用被放大**: 净收益-0.99的任务，效用从-0.99降到-2.30，**放大2.3倍**
4. **大亏损被截断**: 净收益-100和-10的任务得到相同的效用-20.7，**信息丢失**

**后果**:
- 高价值任务的吸引力大幅下降，智能体倾向选择低价值任务
- 算法可能陷入"平庸均衡"，避免高风险高回报的联盟
- 联盟总价值显著降低（这正是您观察到的现象）

---

### 2.2 优先级权重 rho_m 的双刃剑效应

**公式**:
```matlab
rho_m = tasks(curr_task).priority;
inner_val = rho_m * (V*D - Cost);
```

**问题1: 低优先级任务被过度惩罚**

假设任务价值相同，但优先级不同：

| 任务 | V*D | Cost | 净收益 | rho_m | inner_val | U_m (对数) |
|------|-----|------|--------|-------|-----------|-----------|
| 高优先级 | 100 | 50 | 50 | 1.0 | 50 | log(51)≈3.93 |
| 中优先级 | 100 | 50 | 50 | 0.5 | 25 | log(26)≈3.26 |
| 低优先级 | 100 | 50 | 50 | 0.1 | 5  | log(6)≈1.79  |

**效用差异**: 高优先级任务效用是低优先级的 **2.2倍**（而净收益相同！）

**问题2: 优先级与对数的耦合放大误差**

对数函数对 `inner_val` 的敏感度随值域变化：
- 当 `inner_val` 接近0时，对数斜率最大 (d/dx log(1+x) = 1/(1+x))
- 当 `inner_val` 很大时，对数斜率趋近于0

这导致 **低优先级+低收益** 的任务效用被双重压制，而 **高优先级+高收益** 的任务效用反而因对数饱和而增益有限。

---

### 2.3 对数函数不满足可加性（最严重问题）

**数学证明**:

```
log(1 + a) + log(1 + b) = log((1+a)(1+b))
                        = log(1 + a + b + ab)
                        ≠ log(1 + a + b)
```

**影响1: 多任务效用不可拆解**

假设智能体参与2个任务：

**旧公式（线性）**:
```
任务1效用: U1 = 100 - 50 = 50
任务2效用: U2 = 80 - 30 = 50
总效用: U_total = U1 + U2 = 100
```

**新公式（对数）**:
```
任务1效用: U1 = log(1 + 50) ≈ 3.93
任务2效用: U2 = log(1 + 50) ≈ 3.93
总效用: U_total = U1 + U2 ≈ 7.86

但如果直接计算总净收益:
  U_correct = log(1 + 100) ≈ 4.62

误差: 7.86 - 4.62 = 3.24 (68%的误差！)
```

**影响2: 效用差值计算失真**

在 `Preference_gain.m` 中:
```matlab
% 假设智能体从任务A切换到任务B
delta_self = u_n_Q - u_n_P

% 旧公式:
delta_self = (Revenue_Q - Cost_Q) - (Revenue_P - Cost_P)
           = ΔRevenue - ΔCost  (可解释为增量)

% 新公式:
delta_self = log(1 + x_Q) - log(1 + x_P)
           = log((1+x_Q)/(1+x_P))  (比值的对数，不是增量！)
```

**后果**:
- Metropolis准则 `P(accept) = exp(ΔU/T)` 基于能量差的物理模型，要求 `ΔU` 是真正的增量
- 对数效用的差值 `log(Q) - log(P)` 是**比率**而非**差值**，破坏了统计力学基础
- 导致SA算法接受概率失准，收敛性无法保证

**影响3: BMBT偏好计算的系统性偏差**

```matlab
% Preference_gain.m 第125行
deltaU = delta_self + sum_gain_new - sum_loss_old + sum_diff_other;
```

各项拆解：
- `delta_self`: 自身效用变化 (对数差)
- `sum_gain_new`: 新队友的增益 (对数差之和)
- `sum_loss_old`: 旧队友的损失 (对数差之和)
- `sum_diff_other`: 稳定任务队友的影响 (对数差之和)

**问题**: 这4项都是对数差值，直接相加违反了对数运算规则，产生**非线性累积误差**。

**极端案例**:
```
假设智能体参与10个任务，每个任务净收益都是10:

旧公式总效用: 10 * 10 = 100
新公式总效用: 10 * log(1+10) ≈ 10 * 2.40 = 24

现在该智能体退出一个任务:

旧公式: ΔU = -10 (直接减少10%)
新公式: ΔU = 24 - 9*log(11) ≈ 24 - 21.6 = 2.4

新公式的效用变化是旧公式的 2.4/10 = 24%，高估了退出成本！
```

这导致：
- 智能体**过度恐惧**退出任务（损失被夸大）
- 智能体**低估收益**加入高价值任务（收益被压缩）
- 联盟形成陷入僵化，最终总价值降低

---

### 2.4 对数截断导致信息丢失

**代码**:
```matlab
inner_val = rho_m * (V*D - Cost);
U_m = log(1 + max(inner_val, -1 + tol));  % tol = 1e-9
```

**截断规则**:
- 若 `inner_val < -1 + 1e-9`，强制设为 `-1 + 1e-9`
- 对应效用下界: `log(1e-9) ≈ -20.7`

**问题**: 所有 `inner_val ≤ -1` 的任务都得到相同的最低效用

**示例**:

| 任务 | V*D | Cost | 净收益 | inner_val (rho=1) | 截断后 | U_m |
|------|-----|------|--------|-------------------|--------|-----|
| A    | 10  | 15   | -5     | -5                | -0.999999999 | -20.7 |
| B    | 10  | 50   | -40    | -40               | -0.999999999 | -20.7 |
| C    | 10  | 1000 | -990   | -990              | -0.999999999 | -20.7 |

**后果**:
- 任务A是"小亏"（成本略高于收益）
- 任务C是"巨亏"（成本是收益的100倍）
- 但它们的效用完全相同！智能体无法区分"微亏"和"巨亏"

这导致：
- 智能体可能选择巨亏任务（因为看起来和小亏任务一样）
- 算法无法有效规避高成本陷阱
- 优化过程失去细粒度

---

### 2.5 全局评估与个体效用的不一致性

**现状**:
```
个体效用计算 (calc_agent_total_utility):
  U_n = Σ r_{n,m} * log(1 + rho_m * (V_m*D_m - Cost_m))

全局效用评估 (evaluate_coalition_metrics):
  U_global = (Σ V_m * D_m) - (Σ Cost_i)
```

**理论矛盾**:

在理想情况下，所有个体效用之和应等于全局效用（帕累托最优的必要条件）：
```
Σ_{i=1}^N U_i = U_global
```

但现在：
```
Σ_{i=1}^N log(...) ≠ Linear_sum(...)
```

**数值示例**:

假设3个智能体，2个任务：

| 智能体 | 任务1收益 | 任务1成本 | 任务2收益 | 任务2成本 | 个体效用(对数) |
|--------|-----------|-----------|-----------|-----------|----------------|
| 1      | 50        | 20        | 0         | 0         | log(1+30) ≈ 3.43 |
| 2      | 50        | 20        | 30        | 10        | log(1+30) + log(1+20) ≈ 6.48 |
| 3      | 0         | 0         | 30        | 10        | log(1+20) ≈ 3.04 |

**个体效用之和**: 3.43 + 6.48 + 3.04 = **12.95**

**全局评估**:
- 总收益: (50+50) + (30+30) = 160
- 总成本: (20+20) + (10+10) = 60
- 全局净效用: 160 - 60 = **100**

**结论**: 12.95 vs 100，**数量级完全不匹配**！

**后果**:
- 无法通过个体效用推断全局性能
- 结果记录（`history_data.coalition_utility`）失去意义
- 算法比较失去公平性（不同算法用不同的效用尺度）

---

## 3. 数值示例对比

### 3.1 场景1: 高价值任务的联盟

**任务参数**:
- 任务价值: V = 1000
- 完成度: D = 0.8
- 联盟成本: Cost = 300
- 优先级: rho = 1.0

**旧公式**:
```
U = V*D - Cost = 1000*0.8 - 300 = 500
```

**新公式**:
```
inner_val = 1.0 * (1000*0.8 - 300) = 500
U_m = log(1 + 500) = log(501) ≈ 6.22
```

**压缩比**: 500 / 6.22 ≈ **80.4倍**

**影响**: 高价值任务的吸引力从500降到6.22，可能被低价值任务超越

---

### 3.2 场景2: 多任务智能体的效用拆解误差

**智能体参与3个任务**:

| 任务 | V*D | Cost | 净收益 | 旧公式U | 新公式U |
|------|-----|------|--------|---------|---------|
| 1    | 100 | 50   | 50     | 50      | log(51) ≈ 3.93 |
| 2    | 80  | 30   | 50     | 50      | log(51) ≈ 3.93 |
| 3    | 60  | 10   | 50     | 50      | log(51) ≈ 3.93 |

**旧公式总效用**: 50 + 50 + 50 = **150**

**新公式总效用（按现有代码）**: 3.93 + 3.93 + 3.93 = **11.79**

**理论正确值（如果直接计算总净收益）**: log(1 + 150) ≈ **5.02**

**误差**: 11.79 - 5.02 = 6.77 (误差135%！)

**原因**: 对数不可加，`log(a) + log(b) ≠ log(a+b)`

---

### 3.3 场景3: BMBT效用差值计算的失真

**状态P（旧）**: 智能体在任务A（净收益100）
**状态Q（新）**: 智能体在任务B（净收益120）

**旧公式**:
```
U_P = 100
U_Q = 120
ΔU = 120 - 100 = 20 (增益20，增幅20%)
```

**新公式**:
```
U_P = log(1 + 100) ≈ 4.62
U_Q = log(1 + 120) ≈ 4.80
ΔU = 4.80 - 4.62 = 0.18 (增益0.18，增幅3.9%)
```

**后果**:
- 旧公式认为有20%的提升，智能体会倾向切换
- 新公式认为只有3.9%的提升，智能体可能不切换
- 导致算法陷入次优解

**Metropolis接受概率对比** (T=10):
```
旧公式: P(accept) = exp(20/10) = exp(2) ≈ 0.865 (86.5%接受)
新公式: P(accept) = exp(0.18/10) = exp(0.018) ≈ 0.518 (51.8%接受)
```

结论: 新公式使得SA算法**探索性严重不足**，容易陷入局部最优

---

### 3.4 场景4: 亏损任务的信息丢失

**3个亏损任务**:

| 任务 | V*D | Cost | 净收益 | 旧公式U | 新公式U (rho=1) |
|------|-----|------|--------|---------|-----------------|
| X    | 50  | 55   | -5     | -5      | log(1-5)=log(-4) → 截断到log(1e-9) ≈ -20.7 |
| Y    | 50  | 100  | -50    | -50     | log(1-50)=log(-49) → 截断到log(1e-9) ≈ -20.7 |
| Z    | 50  | 500  | -450   | -450    | log(1-450)=log(-449) → 截断到log(1e-9) ≈ -20.7 |

**旧公式**: 可以清晰区分任务X(小亏)、Y(中亏)、Z(巨亏)

**新公式**: 三者效用完全相同，无法区分！

**后果**: 智能体可能误入巨亏任务Z，因为它看起来和小亏任务X一样

---

## 4. 建议方案

### 方案A: 回退到线性公式（最简单）

**修改**: 恢复旧版效用计算

```matlab
% UtilityEvaluator.m 第227-228行
% 旧代码：
% inner_val = rho_m * (V_m * D_m - coalition_cost);
% U_m = log(1 + max(inner_val, -1 + tol));

% 新代码（回退）：
U_m = rho_m * (V_m * D_m - coalition_cost);
```

**优点**:
- 效用值域不被压缩
- 满足可加性，效用差值有物理意义
- 全局评估与个体效用一致
- 不丢失亏损任务的细粒度信息

**缺点**:
- 无对数平滑，无法避免负效用（但这可能是合理的，因为亏损联盟本来就应该有负效用）

---

### 方案B: 统一所有函数使用对数公式（保持一致性）

**需要修改的文件**:

1. **Huo2025算法** (`Value_utility.m` 第85行):
```matlab
% 旧代码：
agentutility = revenue - cost;

% 新代码：
inner_val = rho_m * (revenue - cost);
agentutility = log(1 + max(inner_val, -1 + tol));
```

2. **全局评估函数** (`UtilityEvaluator.m` 第311行):
```matlab
% 旧代码：
global_net_utility = total_completed_value - total_global_cost_sum;

% 新代码（需要重新设计）：
% 选项1: 对全局收益也应用对数
total_inner_val = rho_global * (total_completed_value - total_global_cost_sum);
global_net_utility = log(1 + max(total_inner_val, -1 + tol));

% 选项2: 汇总所有个体效用（但需要解决double-counting问题）
global_net_utility = sum(all_agent_utilities) / N;  % 平均或加权？
```

3. **效用差值计算** (`Preference_gain.m`):
```matlab
% 需要重新推导BMBT公式，适配对数效用
% 可能需要改为比率形式：
deltaU_ratio = exp(u_n_Q) / exp(u_n_P);  % 比率判定
% 若 deltaU_ratio > 1，则接受
```

**优点**:
- 保持对数平滑的优势（防止极端值）
- 全系统一致性

**缺点**:
- 仍然存在对数不可加性问题
- 需要大量修改和测试
- BMBT理论基础可能失效
- 效用压缩导致总价值降低（核心问题未解决）

---

### 方案C: 混合方案——任务级对数+智能体级线性

**思路**: 在任务层面使用对数平滑（避免单个任务极端值），但在智能体总效用层面使用线性求和

```matlab
% UtilityEvaluator.m 修改

% 任务级效用：保留对数平滑
for idx = 1:length(task_list)
    curr_task = task_list(idx);

    % ... (计算 V_m, D_m, coalition_cost)

    % 任务级效用（对数）
    inner_val = rho_m * (V_m * D_m - coalition_cost);
    U_m_smooth = log(1 + max(inner_val, -1 + tol));

    % 但映射回线性值用于汇总（指数还原）
    U_m_linear = exp(U_m_smooth) - 1;  % 或者直接用 inner_val

    % 智能体贡献权重
    r_nm = OCFUtils.calc_resource_contribution_ratio(SC_task, agent_id, participants);
    u_nm = r_nm * U_m_linear;  % 使用线性值

    task_utilities(curr_task) = u_nm;
    agentutility = agentutility + u_nm;  % 线性求和
end
```

**优点**:
- 对数平滑防止单任务极端值影响
- 总效用仍然满足可加性
- 效用差值有物理意义

**缺点**:
- 逻辑复杂，容易出错
- `exp(log(1+x)) - 1 ≈ x` 相当于回到线性公式

---

### 方案D: 调整rho_m和截断阈值（微调方案）

**修改1**: 放宽截断阈值，保留更多负值信息
```matlab
% 当前代码：
U_m = log(1 + max(inner_val, -1 + tol));  % 截断到-1

% 修改后：允许更小的值
U_m = log(1 + max(inner_val, -0.99));  % 放宽到-0.99
% 或者直接用绝对值后取符号：
U_m_abs = log(1 + abs(inner_val)) * sign(inner_val);
```

**修改2**: 归一化 rho_m，避免优先级权重过度放大效应
```matlab
% 当前代码：
rho_m = tasks(curr_task).priority;  % 可能是 [0.1, 1.0]

% 修改后：归一化到均值为1
rho_normalized = rho_m / mean([tasks.priority]);
inner_val = rho_normalized * (V_m * D_m - coalition_cost);
```

**优点**:
- 微调优化，无需大改
- 保留对数平滑框架

**缺点**:
- 无法解决对数不可加性的根本问题
- 效用压缩仍然存在

---

### 方案E: 理论重构——基于效用比率的新BMBT公式

**核心思想**: 既然对数效用的差值是比率而非增量，那么重新设计决策准则

**旧判定准则** (基于增量):
```matlab
if deltaU > 0
    accept();
end
```

**新判定准则** (基于比率):
```matlab
% 对数效用差 = 比率的对数
delta_log = u_n_Q - u_n_P;
ratio = exp(delta_log);  % 效用比率

% 判定：若新状态效用是旧状态的1.05倍以上，则接受
if ratio > 1.05
    accept();
end
```

**修改 `Preference_gain.m`**:
```matlab
% 第125行修改前：
deltaU = delta_self + sum_gain_new - sum_loss_old + sum_diff_other;

% 修改后：改为乘法形式（对数空间的加法 = 原空间的乘法）
log_ratio_self = delta_self;
log_ratio_team = sum_gain_new - sum_loss_old + sum_diff_other;
total_ratio = exp(log_ratio_self + log_ratio_team);

% 返回比率而非差值
deltaU = total_ratio - 1;  % 转换为"相对增益"
```

**优点**:
- 适配对数效用的数学性质
- 保留对数平滑的优势
- 理论自洽

**缺点**:
- 需要重新推导整个BMBT理论
- SA温度调度需要重新设计
- 工作量巨大

---

## 5. 根本原因总结

### 核心矛盾：对数平滑 vs 联盟形成的可加性需求

**对数平滑的初衷**:
- 防止 `log(≤0)` 域错误
- 避免极端值主导决策
- 引入效用饱和效应（经济学中的边际递减）

**但联盟形成算法的要求**:
- **可加性**: 多任务效用需要简单求和
- **线性性**: 效用差值需要是真实增量，而非比率
- **细粒度**: 需要区分不同程度的亏损

**结论**: **对数平滑与现有算法框架根本不兼容**

---

### 为什么总价值降低？

1. **高价值任务被压缩**: 净收益1000的任务，效用只有6.9，吸引力降低80倍
2. **优先级权重放大误差**: 低优先级任务效用被过度压制
3. **效用差值失真**: SA和BMBT的探索性严重不足，陷入局部最优
4. **信息丢失**: 亏损任务无法区分，智能体误入巨亏陷阱

---

## 6. 最终建议

### 推荐方案：回退到线性公式（方案A）

**理由**:
1. **问题根源**: 对数平滑与联盟形成算法的数学基础不兼容
2. **实验证据**: 您的观察"完成的联盟价值非常低"验证了理论分析
3. **成本最低**: 只需恢复一行代码，无需修改其他配套函数
4. **理论清晰**: 线性效用有明确的物理意义（收益-成本）

**如果必须保留对数平滑，建议**:
- 仅在**显示层**使用对数（例如绘图时对Y轴取对数）
- 或仅在**异常检测**时使用对数（如检测效用异常波动）
- **核心计算仍用线性公式**

---

### 如果坚持使用对数公式，必须完成以下配套修改：

1. **统一Huo2025算法** (修改 `Value_utility.m`)
2. **重新设计全局评估函数** (修改 `evaluate_coalition_metrics`)
3. **重构BMBT偏好计算** (修改 `Preference_gain.m`，可能需要改用比率判定)
4. **调整SA温度调度** (因为效用值域变化，温度参数需要重新调优)
5. **修改所有阈值判定** (例如收敛判定、稳定性检测)
6. **重新验证算法收敛性** (对数效用可能破坏原有的收敛保证)

**预计工作量**: 2-3周全职工作 + 大量测试

---

## 7. 补充：为什么注释的旧公式表现更好？

**旧公式** (第104行注释):
```matlab
agentutility = total_revenue - total_cost;
```

**优势**:
1. **值域未压缩**: 高价值任务效用保持1000量级
2. **满足可加性**: `U_total = U1 + U2 + U3` 成立
3. **效用差值清晰**: `ΔU = ΔRevenue - ΔCost` 有明确物理意义
4. **细粒度区分**: 亏损-5和亏损-500可以清晰区分
5. **全局一致性**: 个体效用之和 = 全局净效用

**劣势**:
1. 允许负效用（但这在经济学上是合理的）
2. 无对数平滑（但实际场景中可能不需要）

**为什么新公式表现差？**
- 高价值任务被过度压缩 → 智能体不愿参与高价值联盟
- 效用差值失真 → SA探索性不足 → 陷入局部最优
- 优先级权重放大误差 → 低优先级任务被过度惩罚
- 最终导致形成的联盟都是"保守的低价值联盟"，总价值自然很低

---

## 附录：关键代码位置速查

| 问题 | 文件 | 行号 | 修改建议 |
|------|------|------|----------|
| 对数效用计算 | `Main_fun/UtilityEvaluator.m` | 227-228 | 回退到线性 |
| Huo2025旧版效用 | `comalg/Com_Huo2025/Value_utility.m` | 85 | 统一使用对数或统一用线性 |
| 全局评估不一致 | `Main_fun/UtilityEvaluator.m` | 311 | 改为对数或保持线性 |
| BMBT效用差值 | `SA/Preference_gain.m` | 125 | 改为比率判定或保持线性 |
| 截断阈值 | `Main_fun/UtilityEvaluator.m` | 228 | 放宽或移除截断 |
| 优先级权重 | `Main_fun/UtilityEvaluator.m` | 223, 227 | 归一化rho_m |

---

**报告完成日期**: 2026-03-07
**建议优先级**: 高（核心算法问题，影响所有实验结果）
**预计修复时间**: 方案A(回退) 1小时；方案B(统一) 2-3周
