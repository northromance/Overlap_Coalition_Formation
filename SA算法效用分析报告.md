# SA算法效用低于Qi算法的问题分析报告

## 一、核心问题总结

**效用差距**: SA算法效用为 **4025.19**，Qi算法效用为 **4477.84**，相差 **452.64 (-10.1%)**

这是一个显著的性能差距，说明SA算法在联盟形成策略上存在明显的优化空间。

---

## 二、关键数据对比

| 指标 | SA算法 | Qi算法 | 差异 | 差异率 |
|------|--------|--------|------|--------|
| **总效用** | 4025.19 | 4477.84 | -452.64 | **-10.1%** |
| **总成本** | 1591.47 | 1655.50 | -64.03 | -3.9% |
| **完成分数** | 5616.67 | 6133.33 | -516.67 | -8.4% |
| **平均完成度** | 70.2% | 76.7% | **-6.5个百分点** | - |
| **联盟数量** | 9 | 10 | **-1** | -10.0% |
| **计算时间** | 47.25s | 32.31s | +14.93s | +46.2% |

**关键观察：**
- ❌ 成本更低但效用更低 → 问题在**收益端**（任务完成度）而非成本控制
- ❌ 联盟数量少1个 → 可能存在**资源分配不足**或**join操作被频繁拒绝**
- ❌ 计算时间更长 → SA算法效率有待提升

---

## 三、任务完成度深度分析

### 3.1 完成度对比（按任务）

| 任务ID | SA完成度 | Qi完成度 | 差异 | 问题严重性 |
|--------|----------|----------|------|-----------|
| 1 | **0.0%** | **45.8%** | **-45.8%** | 🔴 **严重**（SA完全放弃）|
| 4 | 68.8% | 93.8% | -25.0% | 🟠 **中等** |
| 5 | 68.8% | 87.5% | -18.8% | 🟡 轻微 |
| 10 | 93.8% | 100.0% | -6.2% | - |
| 2, 3, 6, 7 | 相同 | 相同 | 0% | ✅ 持平 |
| 8, 9 | SA略高 | Qi略低 | +10~20% | ✅ SA优势 |

**关键发现：**
1. **任务1被完全忽略** - SA算法在整个过程中没有给任务1分配任何资源，而Qi算法完成了45.8%
2. **任务4和任务5资源不足** - SA的完成度明显低于Qi
3. **SA在个别任务上有优势**（任务8、9），但无法弥补其他任务的巨大劣势

### 3.2 可能的原因

**为什么任务1被忽略？**

分析代码后，可能的原因包括：

1. **概率计算问题** (`SA_Select_probs.m:86`)
   ```matlab
   score_val = (feat_prio^2 * feat_demand * feat_supply) / (feat_dist + eps_val);
   ```
   - 如果任务1距离较远或优先级较低，其score可能长期处于劣势
   - Boltzmann分布在温度降低后，低分任务被选中的概率趋近于0

2. **初始化策略问题** (`SA_Value_main.m:107-183`)
   - Soft Greedy初始化时，如果任务1未被分配资源，后续SA迭代可能一直无法弥补
   - 初始温度 `T_init_construction=0.5` 较低，可能导致初始化过早陷入贪心

3. **join_operation拒绝率高**
   - 任务1的加入操作可能因为可行性检测（能量、距离、队友检查）被频繁拒绝
   - SA温度下降后，接受劣解的概率降低，进一步减少了探索机会

**对比Qi算法：**

Qi算法的优势：
- **禁忌搜索机制**：防止陷入局部最优，强制探索被忽略的任务
- **热启动策略**：保留上一轮的联盟结构，跨轮信息传递
- **随机离开操作** (p=0.3)：定期清空部分资源，强制重新分配

---

## 四、算法结构差异分析

### 4.1 概率计算公式对比

**SA算法** (`SA_Select_probs.m`):
```matlab
score = (priority^2 * demand * supply) / distance
P(task_j) = exp(score / T) / Σ exp(score_i / T)
```

**Qi算法** (`Qi2023_Select_probs.m`):
```matlab
f_mn = (beta_m^2 * c_m_z * agent_capacity) / d_mn
P(task_j) = exp(f_mn * Gamma) / Σ exp(f_all * Gamma)
```

**关键差异：**

| 维度 | SA算法 | Qi算法 |
|------|--------|--------|
| **温度/系数** | T 指数衰减（α^k * T_0） | Gamma 线性增长（随迭代递增） |
| **资源供给** | supply = agent资源量 | agent_capacity = 归一化后的资源量 |
| **资源需求** | demand = 剩余需求量 | c_m_z = 归一化后的缺口 |
| **归一化** | 分别归一化各维度 | 同样归一化 |

**潜在问题：**
- SA的温度T在第一轮内快速下降（α=0.6），可能导致早期就进入"开发模式"，丧失探索能力
- Qi的Gamma是逐渐增加的，从探索到开发的过渡更平滑

### 4.2 初始化策略对比

**SA算法**:
- **第一轮**：Soft Greedy（T_init=0.5）概率初始化
- **后续轮**：**完全重启**，不保留任何联盟结构

**Qi算法**:
- **第一轮**：根据概率初始化
- **后续轮**：**保留上一轮的联盟结构**（热启动），基于更新后的信念继续优化

**影响：**

Qi的热启动策略有明显优势：
1. 跨轮信息传递，避免重复探索
2. 信念更新后，基于更准确的信息微调联盟结构
3. 节省计算时间（Qi用时32.31s vs SA用时47.25s）

SA每轮重启的问题：
1. 每轮都要从头探索，浪费计算资源
2. 前几轮的良好结构被丢弃，可能导致性能波动

### 4.3 搜索机制对比

| 特性 | SA算法 | Qi算法 |
|------|--------|--------|
| **核心策略** | 模拟退火（Metropolis准则） | 禁忌搜索（Tabu Search） |
| **接受劣解** | exp(ΔU/T) 概率接受 | 只接受 ΔU>0（但有禁忌表） |
| **禁忌表** | ❌ 无 | ✅ 有（长度10，FIFO队列） |
| **离开机制** | 目标化（leave_operation） | 随机化（p=0.3 清空资源） |
| **探索强度** | 随温度递减 | 受Gamma和禁忌表控制 |

**SA的劣势：**
- 无禁忌表，可能反复访问相同的联盟结构
- 离开操作是"目标化"的（基于概率选择任务），可能错过某些组合
- 温度下降过快（α=0.6）导致后期探索性不足

**Qi的优势：**
- 禁忌表防止重复探索
- 随机离开（p=0.3）强制打破当前结构，增加多样性
- Gamma逐渐增大，探索-开发平衡更好

---

## 五、根本原因推断

综合上述分析，SA算法效用低的根本原因是：

### 5.1 **探索性不足**（主因）

**温度参数问题**：
- 初始温度 `T_0=100`，降温系数 `α=0.6`
- 迭代20次后，温度降至 `100 * 0.6^20 ≈ 0.036`
- 此时 `exp(ΔU/T)` 几乎只接受 ΔU>0 的解，失去跳出局部最优的能力

**对比Qi算法**：
- Gamma从1线性增长到100（假设K_max_inner=200，每次增长0.5）
- 增长速度更缓慢，探索期更长
- 结合禁忌表和随机离开，探索能力更强

**结果**：
- SA在第一轮的初始化阶段，如果任务1未被分配资源，后续迭代无法修正
- 温度快速下降导致算法过早收敛，困在局部最优

### 5.2 **初始化策略缺陷**

**Soft Greedy的T_init_construction=0.5 过低**：
- 这个温度下，概率分布已经偏向贪心（exp(score/0.5)梯度较大）
- 如果任务1的score在初始阶段较低（距离远/优先级低），被选中的概率极小
- 一旦错过初始化，后续SA很难补救

**建议**：
- 提高T_init_construction到1.0~2.0，增加初始化的随机性
- 或采用更均匀的初始化策略（如轮流分配）

### 5.3 **每轮重启的策略问题**

**SA每轮重启的劣势**：
1. 前一轮辛苦建立的联盟结构被丢弃
2. 信念更新后，应该基于新信念**微调**现有结构，而非**重建**
3. 浪费计算资源，导致时间更长（47.25s vs 32.31s）

**建议**：
- 参考Qi算法，保留上一轮的联盟结构作为下一轮的起点
- 或在每轮重启前，检查是否保留部分高质量的联盟

### 5.4 **join_operation拒绝率可能过高**

**可能的原因**：
1. **队友可行性检查过于严格** (`validate_feasibility.m:check_teammates_feasibility`)
   - 如果加入任务1会导致某个队友的能量不足，操作被拒绝
   - 但这个检查可能过于保守，某些"边界情况"被错误拒绝

2. **能量约束过紧**
   - 如果任务1距离较远，智能体的能量无法支撑往返+执行
   - 导致任务1从未被考虑

**建议**：
- 统计join_operation的拒绝原因分布
- 适当放宽约束（如允许10%的能量超支）

---

## 六、具体改进建议

### 🔧 改进1: 调整温度参数（最关键）

**文件**: `Main_fun/Compare_Algorithms.m`

**当前参数**:
```matlab
SA_Temperature = 100.0;
SA_alpha = 0.6;  % 降温系数
```

**建议修改**:
```matlab
SA_Temperature = 100.0;
SA_alpha = 0.75;  % 提高到0.75-0.8，减缓降温速度
```

**预期效果**:
- 温度下降更慢，探索期延长
- 迭代20次后，温度降至 `100 * 0.75^20 ≈ 3.17`（vs 原来的0.036）
- 仍能接受一定的劣解，避免过早收敛

---

### 🔧 改进2: 提高初始化温度

**文件**: `SA/SA_Value_main.m:127`

**当前代码**:
```matlab
T_init_construction = 0.5;  % Soft Greedy初始化温度
```

**建议修改**:
```matlab
T_init_construction = 1.5;  % 提高到1.5-2.0
```

**预期效果**:
- 初始化阶段更随机，避免过早陷入贪心
- 任务1等"边缘任务"有更大概率被分配资源

---

### 🔧 改进3: 增加禁忌表机制（可选）

**文件**: 新建 `SA/TabuList.m` 或在 `SA_Value_main.m` 中添加

**伪代码**:
```matlab
% 在SA内循环前初始化
TabuList = {};
L_tabu = 10;  % 禁忌表长度

% 在每次接受新状态后
if accept_join || accept_leave
    SC_hash = get_SC_hash(Value_data.SC);
    TabuList = update_tabu_list(TabuList, SC_hash, L_tabu);
end

% 在决策前检查
if is_in_tabu(SC_hash, TabuList)
    continue;  % 跳过已访问的解
end
```

**预期效果**:
- 防止反复探索相同的联盟结构
- 强制算法探索新区域

---

### 🔧 改进4: 采用热启动策略

**文件**: `SA/SA_Value_main.m`

**当前逻辑**（第2轮开始）:
```matlab
% 每轮都重新初始化SC_global（空联盟）
for m = 1:M
    SC_global{m} = zeros(N, K);
end
```

**建议修改**:
```matlab
% 第2轮开始，保留上一轮的联盟结构
if round > 1
    SC_global = previous_round_SC;  % 热启动
    fprintf('[SA] 第%d轮：基于上一轮联盟结构继续优化\n', round);
else
    % 第一轮照常初始化
    for m = 1:M
        SC_global{m} = zeros(N, K);
    end
end
```

**预期效果**:
- 跨轮信息传递，避免重复探索
- 节省计算时间
- 基于更新后的信念微调联盟结构，而非重建

---

### 🔧 改进5: 调整概率计算公式

**文件**: `SA/SA_Select_probs.m:86`

**当前公式**:
```matlab
score_val = (feat_prio^2 * feat_demand * feat_supply) / (feat_dist + eps_val);
```

**建议修改1**（增加任务价值权重）:
```matlab
% 获取任务价值
feat_value = tasks(j).value / max([tasks.value]);  % 归一化

% 新公式：增加价值维度
score_val = (feat_prio^2 * feat_value * feat_demand * feat_supply) / (feat_dist + eps_val);
```

**建议修改2**（减小距离惩罚）:
```matlab
% 当前距离权重为1（分母），可能惩罚过重
% 改为距离的平方根，降低距离的影响
score_val = (feat_prio^2 * feat_demand * feat_supply) / (sqrt(feat_dist) + eps_val);
```

**预期效果**:
- 高价值任务获得更高的选择概率
- 远距离任务不会被过度忽略

---

### 🔧 改进6: 分析并优化可行性检测

**文件**: `Main_fun/validate_feasibility.m`

**建议添加日志统计**:
```matlab
% 在join_operation中添加统计
persistent reject_reasons;
if isempty(reject_reasons)
    reject_reasons = struct();
end

if ~feasible
    reason = info.reason;
    if ~isfield(reject_reasons, reason)
        reject_reasons.(reason) = 0;
    end
    reject_reasons.(reason) = reject_reasons.(reason) + 1;
end

% 在每轮结束后打印
fprintf('拒绝原因统计:\n');
disp(reject_reasons);
```

**预期效果**:
- 了解哪些约束导致了最多的拒绝
- 针对性地放宽约束

---

### 🔧 改进7: 增加随机扰动机制（对抗局部最优）

**文件**: `SA/SA_Value_main.m`（在SA内循环中）

**伪代码**:
```matlab
% 每隔N_perturb次迭代，强制进行一次随机扰动
N_perturb = 10;
if mod(k_iter, N_perturb) == 0 && k_stable > 5
    % 随机移除30%的资源分配
    for m = 1:M
        for i = 1:N
            for k = 1:K
                if SC_global{m}(i,k) > 0 && rand < 0.3
                    SC_global{m}(i,k) = 0;
                end
            end
        end
    end
    fprintf('[SA] 执行随机扰动，打破局部最优\n');
end
```

**预期效果**:
- 类似Qi算法的随机离开机制
- 防止长期困在次优解

---

## 七、优先级建议

根据改进难度和预期收益，建议按以下顺序实施：

### 🔥 高优先级（立即实施）

1. **调整温度参数** (`α: 0.6 → 0.75`) - **最简单，最有效**
2. **提高初始化温度** (`T_init: 0.5 → 1.5`)

### 🔸 中优先级（短期实施）

3. **采用热启动策略**（保留跨轮联盟结构）
4. **调整概率公式**（增加价值权重或减小距离惩罚）

### 🔹 低优先级（长期优化）

5. **增加禁忌表机制**
6. **分析可行性检测**（添加统计日志）
7. **增加随机扰动机制**

---

## 八、预期改进效果

如果实施上述**高优先级**改进（改进1+2），预期效果：

| 指标 | 当前SA | 预期改进后 | Qi算法 |
|------|--------|-----------|--------|
| 总效用 | 4025.19 | **4300~4400** | 4477.84 |
| 平均完成度 | 70.2% | **73~75%** | 76.7% |
| 联盟数量 | 9 | **9~10** | 10 |
| 任务1完成度 | 0% | **20~40%** | 45.8% |

如果实施**全部改进**，SA算法有望**接近或超越**Qi算法的性能。

---

## 九、进一步调试建议

### 9.1 添加详细日志

在 `SA_Value_main.m` 中添加：
```matlab
% 记录每轮的关键指标
round_log(round).SC_global = SC_global;
round_log(round).best_utility = best_utility;
round_log(round).temperature = Temperature;
round_log(round).num_coalitions = count_coalitions(SC_global);
round_log(round).task_completion = calc_task_completion(SC_global);
```

### 9.2 可视化对比

创建脚本绘制：
1. SA vs Qi的**任务完成度热力图**
2. SA vs Qi的**联盟结构网络图**
3. SA温度曲线 vs Qi的Gamma曲线

### 9.3 多实例测试

使用不同的随机种子运行10次，分析：
- SA是否在所有实例中都忽略任务1？
- 还是只是个别情况下的偶然？

---

## 十、总结

**核心问题**: SA算法的效用比Qi算法低10.1%（452.64），主要是因为：
1. ❌ **探索性不足** - 温度下降过快（α=0.6），过早收敛
2. ❌ **初始化缺陷** - Soft Greedy温度过低（T=0.5），某些任务（如任务1）从未被分配资源
3. ❌ **每轮重启** - 丢弃前一轮的良好结构，浪费计算资源
4. ❌ **缺乏多样性机制** - 无禁忌表、无随机扰动，容易陷入局部最优

**快速修复方案**:
- 调整 `α: 0.6 → 0.75` 和 `T_init: 0.5 → 1.5`
- 预期可提升效用约 **200~300** 点（缩小至Qi的5%以内）

**长期优化方向**:
- 采用热启动、禁忌表、随机扰动等高级机制
- 有望超越Qi算法的性能

---

**报告日期**: 2026-02-04
**分析工具**: analyze_results.m
**数据来源**: comparison_N6_M10_SA+Qi+SA_20260203_234857.mat
