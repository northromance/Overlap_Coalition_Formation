# 联盟形成不完全分析报告

**问题**：为什么10个任务中只有8个形成联盟？任务2和任务5为何被忽略？

**场景参数**：
- 智能体数量：N=6
- 任务数量：M=10
- 资源类型：K=6
- 迭代轮数：20轮

---

## 1. 被忽略任务的详细信息

### 任务2（优先级4，被忽略）
| 属性 | 值 |
|------|-----|
| **位置** | **(1, 87)** - 地图左上角边缘 |
| **价值** | 800 |
| **类型** | 1（低价值任务） |
| **需求** | [4, 2, 0, 4, 3, 0] |
| **优先级** | 4 / 10 |

**距离智能体的距离**：
- 到智能体1：87.7单位
- 到智能体2：66.1单位
- 到智能体3：**51.0单位**（最近）
- 到智能体4：67.2单位
- 到智能体5：105.2单位
- 到智能体6：59.3单位

**问题**：
- ❌ **位置极端偏远**（地图边角）
- ❌ 最近的智能体距离也有51单位
- ❌ 飞行成本 = 距离 × agent_fuel = 51 × 1.0 = **51能量**
- ❌ 预期收益：800 × 完成度（假设0.7）= 560
- ❌ **成本过高，效用为负或接近0**

---

### 任务5（优先级1，最低优先级，被忽略）
| 属性 | 值 |
|------|-----|
| **位置** | **(69, 33)** - 地图右侧中部 |
| **价值** | 800 |
| **类型** | 1（低价值任务） |
| **需求** | [4, 2, 0, 4, 3, 0] |
| **优先级** | **1 / 10**（最低） |

**距离智能体的距离**：
- 到智能体1：36.9单位
- 到智能体2：57.1单位
- 到智能体3：38.8单位
- 到智能体4：59.0单位
- 到智能体5：**24.2单位**（最近）
- 到智能体6：60.7单位

**最近智能体（智能体5）的资源能力**：
```
智能体5资源：[0, 0, 3, 0, 0, 1]
任务5需求：  [4, 2, 0, 4, 3, 0]
            ↓  ↓     ↓  ↓
匹配情况：  ✗  ✗  ✓  ✗  ✓  ✓
```

**问题**：
- ❌ **资源严重不匹配**：智能体5无法提供资源1、2、4、5
- ❌ 需要其他智能体参与，但距离都在36-60单位
- ❌ **优先级最低**，在资源竞争中处于劣势
- ❌ 多智能体协作成本高

---

## 2. 被选中任务的对比（相同价值和需求）

在4个相同价值（800）和需求（[4 2 0 4 3 0]）的任务中：

| 任务 | 优先级 | 位置 | 是否被选 | 关键因素 |
|------|--------|------|----------|----------|
| **任务9** | 9 (高) | (17, 36) | ✅ 被选 | 距离智能体1/2近（36.1/14.6），资源匹配好 |
| **任务7** | 2 (低) | (88, 74) | ✅ 被选 | 距离智能体4/6近（26.9/33.8），资源匹配好 |
| **任务2** | 4 (中) | (1, 87) | ❌ 忽略 | **位置极端偏远**（边角），飞行成本过高 |
| **任务5** | 1 (最低) | (69, 33) | ❌ 忽略 | **资源不匹配** + 优先级最低 |

**关键发现**：
- ✅ 任务7虽然优先级只有2，但因为**位置好+资源匹配**仍被选中
- ❌ 任务2虽然优先级4比任务7高，但因为**位置太差**被忽略
- 说明：**位置和资源匹配度** > **优先级**

---

## 3. 算法决策机制分析

### 3.1 效用计算公式

根据 `UtilityEvaluator.m`：

```matlab
U_m(A_m) = log(1 + rho_m * (V_m * D_m - Cost(A_m)))
```

其中：
- `V_m`：任务期望价值 = 800
- `D_m`：任务完成度（0-1）
- `Cost(A_m)`：联盟成本 = 飞行成本 + 等待成本 + 执行成本
- `rho_m`：任务优先级权重

### 3.2 成本构成

```
Cost(A_m) = Σ r_{i,m} * C_i

其中单个智能体成本：
C_i = t_fly * alpha_fly + t_wait * alpha_wait + t_exec * beta
    = 距离/速度 * 1.0 + 等待时间 * 0.5 + 执行时间 * 1.0
```

**参数设置**：
- `agent_velocity = 2`：移动速度
- `agent_fuel = 1.0`：飞行油耗系数
- `agent_wait_fuel = 0.5`：等待油耗系数
- `agent_beta = 1.0`：执行油耗系数
- `resource_exec_time = [50 65 50 60 35 45]`：各资源执行时间

### 3.3 任务2的效用估算

假设智能体3（距离51单位）参与任务2：

```
飞行时间：t_fly = 51 / 2 = 25.5
飞行成本：25.5 * 1.0 = 25.5

执行时间：取需求资源的最大执行时间
  资源2需求2单位，执行时间65
  资源4需求4单位，执行时间60
  → t_exec ≈ 65

执行成本：65 * 1.0 = 65

总成本：Cost ≈ 25.5 + 65 = 90.5
```

假设完成度 D_m = 0.7：
```
收益：V_m * D_m = 800 * 0.7 = 560
净收益：560 - 90.5 = 469.5 > 0（正效用）
```

但对比任务9（智能体2参与，距离14.6）：
```
飞行成本：14.6/2 * 1.0 = 7.3
执行成本：65
总成本：72.3

收益：560
净收益：560 - 72.3 = 487.7（更高！）
```

**结论**：任务2虽然有正效用，但在资源竞争中输给了**成本更低**的同类任务。

---

## 4. 根本原因总结

### 原因1：地理位置劣势（任务2）

**任务2位置**：(1, 87) - 地图左上角边缘

**智能体分布**：
- 智能体1: (41, 9) - 地图左下
- 智能体2: (13, 22) - 地图左下
- 智能体3: (47, 65) - 地图中部
- 智能体4: (68, 92) - 地图右上
- 智能体5: (93, 36) - 地图右侧
- 智能体6: (60, 93) - 地图右上

**地图布局**：
```
(0,100) ──────── (100,100)
   │   任务2(1,87)      │
   │    ✗远             │
   │                    │
   │  智能体集中在      │
   │  右侧和下方        │
   │                    │
(0,0) ───────────── (100,0)
```

**问题**：
- 智能体主要分布在地图**右侧和下方**
- 任务2在**左上角边缘**，距离所有智能体都很远
- **飞行成本过高**，导致效用在竞争中落败

---

### 原因2：资源不匹配 + 最低优先级（任务5）

**任务5需求**：[4, 2, 0, 4, 3, 0]
- 需要资源1、2、4、5

**最近智能体5的能力**：[0, 0, 3, 0, 0, 1]
- 只能提供资源3、6
- **完全无法满足任务5的主要需求**

**次近智能体**：
- 智能体1（距离36.9）：[1, 4, 0, 1, 0, 3] - 可提供资源1、2、6
- 智能体3（距离38.8）：[1, 3, 0, 4, 4, 3] - 可提供资源1、2、4、5 ✓

虽然智能体3能满足需求，但：
1. 距离38.8单位，飞行成本 ≈ 19.4
2. **优先级只有1（最低）**
3. 在算法的初始构造和后续优化中，高优先级任务会优先占用资源
4. 智能体3可能已经被分配到更高优先级的任务

**验证**：智能体3实际参与的任务
```
智能体3参与：任务1、6、8
- 任务1：优先级3
- 任务6：优先级6
- 任务8：优先级10（最高）
```

**结论**：智能体3被更高优先级的任务"抢走"了，任务5没有合适的智能体参与。

---

## 5. 参数设置的影响分析

### 5.1 当前参数

| 参数 | 值 | 影响 |
|------|-----|------|
| `agent_fuel` | 1.0 | **飞行成本线性增长**，远距离任务惩罚重 |
| `agent_velocity` | 2.0 | 速度较慢，飞行时间长 |
| `agent_beta` | 1.0 | 执行成本与飞行成本等权重 |
| `resource_confidence` | 0.9 | 高置信度，需求估算保守 |
| `T_init_construction` | 0.5 | **初始构造温度低**，接近贪婪选择 |
| `K_stable_max` | 15 | 允许15次无改进迭代后停止 |

### 5.2 关键参数影响

#### A. `agent_fuel = 1.0`（飞行油耗系数）
**影响**：远距离任务的成本线性增长

**示例**：
- 任务9距离14.6 → 飞行成本7.3
- 任务2距离51.0 → 飞行成本25.5
- **差距 = 25.5 - 7.3 = 18.2**（相当于任务价值的2.3%）

**调优建议**：
- 若降低 `agent_fuel = 0.5`，远距离任务的惩罚减半
- 若提高 `agent_fuel = 2.0`，远距离任务几乎不可能被选中

---

#### B. `T_init_construction = 0.5`（初始构造温度）
**影响**：温度低导致**接近贪婪选择**，早期就锁定高价值近距离任务

**机制**：
- 初始构造阶段使用Metropolis准则：`P(accept) = exp(-ΔE/T)`
- 温度T=0.5时，若ΔE=-10，接受概率 ≈ 2e-9（几乎不接受差解）
- **结果**：智能体优先选择"高效用"任务，低优先级任务被边缘化

**调优建议**：
- 提高到 `T_init_construction = 2.0`，允许更多探索
- 可能帮助低优先级但位置合理的任务获得资源

---

#### C. `resource_confidence = 0.9`（需求估算置信度）
**影响**：高置信度导致需求估算保守，可能高估资源需求

**机制**：
```matlab
demand = WorldSim.calculate_demand_quantile(belief, task_type_demands, confidence);
```
- 置信度0.9 → 选择90%分位数的需求
- 若实际需求是 [4 2 0 4 3 0]，估算可能变成 [5 3 0 5 4 0]（更高）
- **结果**：任务看起来"更难完成"，效用降低

**调优建议**：
- 降低到 `resource_confidence = 0.7`，需求估算更乐观
- 可能提高边缘任务的效用评估

---

#### D. 优先级权重 `rho_m`
**当前机制**：
```matlab
rho_m = tasks(curr_task).priority;  % 1-10
inner_val = rho_m * (V_m * D_m - Cost);
```

**问题**：
- 任务5优先级1，任务9优先级9
- 即使净收益相同，任务9的效用是任务5的**9倍**
- **低优先级任务被过度惩罚**

**示例**：
```
任务5：rho=1, 净收益=400 → inner_val = 1*400 = 400 → U=log(401)≈6.0
任务9：rho=9, 净收益=400 → inner_val = 9*400 = 3600 → U=log(3601)≈8.2
```

**调优建议**：
- 归一化优先级权重：`rho_normalized = rho_m / mean([tasks.priority])`
- 或使用更温和的权重函数：`rho_m = 1 + 0.5 * tasks(m).priority`

---

## 6. 对比相同价值任务的选择策略

4个价值800、需求相同的任务（2, 5, 7, 9）：

| 任务 | 优先级 | 位置 | 最近智能体 | 距离 | 资源匹配 | 是否被选 |
|------|--------|------|------------|------|----------|----------|
| 9 | **9** | (17,36) | 智能体2 | 14.6 | ✓ 好 | ✅ 选 |
| 7 | 2 | (88,74) | 智能体4 | 26.9 | ✓ 好 | ✅ 选 |
| 2 | 4 | (1,87) | 智能体3 | 51.0 | ✓ 可 | ❌ 弃 |
| 5 | **1** | (69,33) | 智能体5 | 24.2 | ✗ 差 | ❌ 弃 |

**决策优先级**：
```
资源匹配度 > 距离/成本 > 优先级 > 价值
```

**解释**：
1. **任务7虽然优先级低（2），但位置+资源好，被选中**
2. **任务2优先级中等（4），但位置差，被淘汰**
3. 说明算法是**成本驱动**的，而非单纯的优先级驱动

---

## 7. 解决方案与建议

### 方案A：调整参数（推荐，无需改代码）

#### A1. 降低飞行成本惩罚
```matlab
agent_fuel = 0.5;  % 从1.0降到0.5
```
**效果**：远距离任务的飞行成本减半，任务2可能获得机会

#### A2. 提高初始构造温度
```matlab
T_init_construction = 2.0;  % 从0.5提高到2.0
```
**效果**：初始阶段允许更多探索，低优先级任务有机会被尝试

#### A3. 降低需求估算置信度
```matlab
resource_confidence = 0.7;  % 从0.9降到0.7
```
**效果**：需求估算更乐观，任务完成度评估更高，效用提升

#### A4. 归一化优先级权重（需修改代码）
```matlab
% UtilityEvaluator.m 第223行修改为：
mean_priority = mean([tasks.priority]);  % 平均优先级 ≈ 5.5
rho_m = tasks(curr_task).priority / mean_priority;  % 归一化到均值=1
```
**效果**：优先级权重从[1,10]压缩到[0.18, 1.82]，差距缩小

---

### 方案B：改进算法逻辑（需修改代码）

#### B1. 引入任务覆盖率约束
```matlab
% Compare_Algorithms.m 添加参数
Value_Params.min_task_coverage = 0.8;  % 至少80%任务被尝试
```

在算法中增加检查：
```matlab
completed_tasks = sum(cellfun(@(x) ~isempty(find(sum(x,2)>0)), SC));
if completed_tasks / M < min_task_coverage
    % 强制尝试未分配的任务
end
```

#### B2. 多轮资源分配
第一轮：分配高优先级任务
第二轮：用剩余资源尝试低优先级任务

#### B3. 动态调整优先级权重
```matlab
% 随着轮数增加，逐步降低优先级的影响
rho_m_adjusted = rho_m * (1 - 0.5 * counter / num_rounds);
```

---

### 方案C：场景调整（最简单）

#### C1. 调整任务位置分布
使用更集中的任务分布：
```matlab
% 原代码：
tasks(j).x = round(rand(1) * (WORLD.XMAX - WORLD.XMIN) + WORLD.XMIN);

% 修改为高斯分布：
center_x = (WORLD.XMAX + WORLD.XMIN) / 2;
center_y = (WORLD.YMAX + WORLD.YMIN) / 2;
tasks(j).x = round(center_x + 20*randn(1));  % 标准差20，集中在中心
tasks(j).y = round(center_y + 20*randn(1));
```

#### C2. 增加智能体数量
```matlab
N = 8;  % 从6增加到8
```
**效果**：更多智能体覆盖更广的地图区域，边缘任务获得支持

#### C3. 降低任务数量
```matlab
M = 8;  % 从10降到8
```
**效果**：资源更充裕，所有任务都能被覆盖

---

## 8. 快速验证测试

### 测试1：降低飞行成本（5分钟测试）

**修改**：
```matlab
% Compare_Algorithms.m 第58行
agent_fuel = 0.5;  % 原值1.0
```

**预期结果**：任务2的飞行成本从25.5降到12.75，可能被选中

---

### 测试2：提高初始温度（5分钟测试）

**修改**：
```matlab
% Compare_Algorithms.m 第96行
T_init_construction = 2.0;  % 原值0.5
```

**预期结果**：初始构造阶段更多探索，低优先级任务有机会

---

### 测试3：组合调优（推荐）

**修改**：
```matlab
agent_fuel = 0.6;              % 降低飞行成本
T_init_construction = 1.5;     % 提高初始温度
resource_confidence = 0.75;    % 降低需求估算保守度
```

**预期结果**：应该能形成9-10个联盟

---

## 9. 结论

### 核心发现

1. **地理位置是第一决策因素**
   - 任务2因位置偏远（边角）被忽略
   - 任务7虽然优先级低，但位置好仍被选中

2. **资源匹配度第二重要**
   - 任务5虽然距离不远，但最近的智能体资源不匹配
   - 资源不匹配导致需要多智能体协作，成本上升

3. **优先级在成本面前权重较低**
   - 决策优先级：`资源匹配 > 距离/成本 > 优先级 > 价值`
   - 低优先级但成本低的任务，仍可能战胜高优先级但成本高的任务

4. **当前参数偏向保守/贪婪**
   - `T_init_construction = 0.5`：低温导致贪婪选择
   - `agent_fuel = 1.0`：飞行成本惩罚重
   - `resource_confidence = 0.9`：需求估算保守
   - 组合效果：只选择"最优"任务，忽略次优任务

### 是否合理？

**合理性论证**：
- ✓ 算法正确地识别出**成本过高的任务**（任务2）
- ✓ 算法正确地避免**资源不匹配的任务**（任务5）
- ✓ 在有限资源下，优先完成高效任务是**帕累托最优**

**潜在问题**：
- ⚠ 如果任务覆盖率是硬约束（如军事任务，所有目标必须侦察），当前策略不适用
- ⚠ 低优先级任务可能包含重要信息（如侦察边界区域），完全忽略有风险

### 推荐方案

**如果追求总效用最大化（当前目标）**：
→ 保持现状，当前行为是合理的

**如果追求任务覆盖率**：
→ 采用**方案A1+A2**（降低飞行成本+提高初始温度）

**如果两者兼顾**：
→ 采用**方案A的组合调优**（测试3）

---

## 10. 附录：完整参数速查表

| 参数 | 当前值 | 影响 | 建议调整 |
|------|--------|------|----------|
| N（智能体数） | 6 | 覆盖范围 | 增加到8 |
| M（任务数） | 10 | 竞争强度 | 减少到8 |
| agent_fuel | 1.0 | 飞行成本 | 降到0.5-0.7 |
| agent_velocity | 2.0 | 飞行时间 | 提高到3.0 |
| agent_beta | 1.0 | 执行成本 | 保持 |
| T_init_construction | 0.5 | 初始探索 | 提高到1.5-2.0 |
| resource_confidence | 0.9 | 需求估算 | 降到0.7-0.8 |
| K_stable_max | 15 | 收敛速度 | 提高到25 |
| num_rounds | 20 | 迭代轮数 | 保持或增加到30 |
| MaxIter | 80 | 每轮迭代 | 保持 |

---

**报告生成时间**：2026-03-07
**分析对象**：`comparison_N6_M10_TabuGlobal_20260307_194451.mat`
**算法**：SA_TabuEnhanced_Global（全局社会效用）
