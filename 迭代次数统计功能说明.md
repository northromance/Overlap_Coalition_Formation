# 迭代次数统计功能说明

## 概述

本次修改为所有算法统一了内循环迭代次数的变量名，并添加了迭代次数的统计和可视化功能。

## 修改内容

### 1. 统一变量名

所有算法的内循环迭代次数变量已统一为 `k_iter`，包括：

| 算法文件 | 原变量名 | 新变量名 | 说明 |
|---------|---------|---------|------|
| **SA_Value_TabuEnhanced_Altruistic_main.m** | `k_iter` | `k_iter` | 保持不变 |
| **SA_Value_TabuEnhanced_main.m** | `k_iter` | `k_iter` | 保持不变 |
| **SA_Value_AdaptiveAlpha_main.m** | `k_iter` | `k_iter` | 保持不变 |
| **Qi2023_main.m** | `k_iter` | `k_iter` | 保持不变 |
| **Fang2025_main.m** | `k_iter` | `k_iter` | 保持不变 |
| **Huo2025_main.m** | `T` | `k_iter` | ✅ 已修改 |
| **Shi2024_main.m** (子函数) | `iteration` | `k_iter` | ✅ 已修改 |
| **Greedy_Baseline_main.m** | - | `k_iter = 1` | ✅ 已添加 |

### 2. 历史数据记录

所有算法现在都会在 `history_data` 中记录每轮的迭代次数：

```matlab
history_data.k_iter_per_round{counter} = k_iter;
```

- 对于有内循环的算法（如 SA、Qi2023、Huo2025、Shi2024），`k_iter` 是实际的迭代次数
- 对于没有内循环的算法（如 Greedy_Baseline），`k_iter = 1`

### 3. 可视化功能

在 `Plot_Results.m` 中添加了迭代次数可视化功能，包括两种图表：

#### 3.1 柱状图（平均迭代次数）
- 显示每个算法在所有轮次中的平均迭代次数
- 适合快速对比不同算法的收敛速度

#### 3.2 折线图（每轮迭代次数变化）
- 显示每个算法在各轮的迭代次数变化趋势
- 可以观察算法在不同轮次的收敛行为

## 使用方法

### 1. 运行算法并生成数据

首先运行 `Compare_Algorithms.m` 生成实验数据：

```matlab
Compare_Algorithms
```

### 2. 查看迭代次数可视化

修改 `Plot_Results.m` 中的配置：

```matlab
plot_config.iterations = true;  % 启用迭代次数图
```

然后运行：

```matlab
Plot_Results
```

### 3. 运行测试

验证迭代次数统计功能是否正常工作：

```matlab
cd tests
test_iteration_counter
```

## 修改的文件列表

### 算法文件

1. `comalg/Com_Huo2025/Huo2025_main.m`
   - 将 `T` 改为 `k_iter`
   - 添加 `history_data.k_iter_per_round{counter} = k_iter;`

2. `comalg/Com_Shi2024/Shi2024_main.m`
   - 将子函数 `optimize_coalitions` 中的 `iteration` 改为 `k_iter`
   - 修改 `initial_coalition_formation` 函数返回 `k_iter = 1`
   - 添加 `history_data.k_iter_per_round{round} = k_iter;`

3. `comalg/SA_TabuEnhance/SA_Value_TabuEnhanced_Altruistic_main.m`
   - 添加 `history_data.k_iter_per_round{counter} = k_iter;`

4. `comalg/SA_TabuEnhance/SA_Value_TabuEnhanced_main.m`
   - 添加 `history_data.k_iter_per_round{counter} = k_iter;`

5. `comalg/SA_Value_Adapt/SA_Value_AdaptiveAlpha_main.m`
   - 添加 `history_data.k_iter_per_round{counter} = k_iter;`

6. `comalg/Com_Qi2023/Qi2023_main.m`
   - 添加 `history_data.k_iter_per_round{round} = k_iter;`

7. `comalg/Com_Fang2025/Fang2025_main.m`
   - 添加 `history_data.k_iter_per_round{counter} = k_iter;`

8. `comalg/Com_Baseline/Greedy_Baseline_main.m`
   - 添加 `history_data.k_iter_per_round{1} = 1;`

### 可视化文件

9. `Plot_Results.m`
   - 添加 `plot_config.iterations` 配置选项
   - 添加迭代次数柱状图绘制代码
   - 添加迭代次数折线图绘制代码

### 测试文件

10. `tests/test_iteration_counter.m`
    - 新增测试文件，验证迭代次数统计功能

## 数据结构说明

### history_data.k_iter_per_round

这是一个 cell 数组，存储每轮的迭代次数：

```matlab
history_data.k_iter_per_round{1} = 5;   % 第1轮迭代了5次
history_data.k_iter_per_round{2} = 3;   % 第2轮迭代了3次
history_data.k_iter_per_round{3} = 4;   % 第3轮迭代了4次
...
```

### 计算平均迭代次数

```matlab
% 提取所有轮次的迭代次数
k_iters = zeros(1, num_rounds);
for r = 1:num_rounds
    k_iters(r) = history_data.k_iter_per_round{r};
end

% 计算平均值
mean_k_iter = mean(k_iters);
```

## 示例输出

### 测试输出示例

```
========================================
  迭代次数统计功能测试
========================================

加载结果文件: comparison_results_seed2456_20260228_123456.mat

========================================
  测试结果
========================================

[SA_Value_TabuEnhanced_Altruistic]
  ✓ k_iter_per_round 字段存在
  - 轮数: 5
  - 平均迭代次数: 12.40
  - 最小迭代次数: 8
  - 最大迭代次数: 15
  - 每轮迭代次数: [12 15 10 8 17]

[Huo2025]
  ✓ k_iter_per_round 字段存在
  - 轮数: 5
  - 平均迭代次数: 6.20
  - 最小迭代次数: 4
  - 最大迭代次数: 8
  - 每轮迭代次数: [7 8 4 6 6]

...

========================================
  测试总结
========================================

✅ 所有测试通过！迭代次数统计功能正常工作。
```

## 注意事项

1. **向后兼容性**：本次修改不影响算法的核心逻辑，仅添加了数据记录和可视化功能

2. **Greedy_Baseline**：由于贪心算法是一次性构建联盟，没有内循环迭代，因此 `k_iter` 固定为 1

3. **Shi2024 的特殊性**：
   - 第一轮使用 `initial_coalition_formation`，k_iter = 1
   - 后续轮使用 `optimize_coalitions`，k_iter 为实际迭代次数

4. **数据格式**：`k_iter_per_round` 是 cell 数组而不是普通数组，这样可以保持与其他 history_data 字段的一致性

## 后续改进建议

1. 可以添加更多统计指标，如：
   - 收敛时间（达到稳定状态的迭代次数）
   - 迭代次数的标准差（反映算法稳定性）

2. 可以添加更多可视化选项，如：
   - 箱线图（显示迭代次数的分布）
   - 热力图（显示不同参数下的迭代次数）

3. 可以将迭代次数作为算法性能的评价指标之一，加入到 comparison_stats 中

## 联系方式

如有问题或建议，请联系项目维护者。

---

**修改日期**: 2026-02-28
**版本**: v1.0
